package run.undead.pubsub;

/**
 * PubSub is an interface for publishing and subscribing to messages on a topic
 * and is a combination of {@link Pub} and {@link Sub} interfaces.
 */
public interface PubSub extends Pub, Sub{ }
